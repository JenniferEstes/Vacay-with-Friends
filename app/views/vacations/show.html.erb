<!--<h1><%#= @user.group_name %> will be traveling on <%#= @vacation.date_traveling%> to <%#= @vacation.description! %> if they have the money!</h1>-->

<h2>Show Vacation:</h2>

<%= @vacation.date_traveling %>
<%= @vacation.description %>

<%= button_to 'Edit', edit_group_vacation_path(@vacation.group, @vacation), method: :get %>
<br>
<%= button_to 'Goals', vacation_goals_path(@vacation), method: :get %>
<%= button_to 'Back to Vacations', group_vacations_path(@vacation.group), method: :get %>
